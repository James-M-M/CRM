@page "/ViewProjects"
@using Microsoft.AspNetCore.Authorization
@using CRM.Data
@using System.Security.Claims
@using Microsoft.EntityFrameworkCore
@attribute [Authorize]
@inject ApplicationDbContext dbContext
@inject NavigationManager navManager
@inject AuthenticationStateProvider AuthenticationStateProvider


<h3>Projects</h3>

<div class="mb-3">
    <button class="btn btn-primary" @onclick="@(() => navManager.NavigateTo("/addproject"))">
        <i class="oi oi-plus"></i> Add New Project
    </button>
</div>

<div class="table-responsive">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Project Name</th>
                <th>Customer</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var project in Projects)
            {
                <tr>
                    <td>@project.Name</td>
                    <td>@project.Customer?.Name</td>
                    <td>
                        <button class="btn btn-primary" @onclick="() => ViewProjectDetails(project.ProjectId)">
                            View Details
                        </button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@code {
    private List<Project> Projects = new();

    protected override async Task OnInitializedAsync()
    {
        Projects = await dbContext.Projects
            .Include(p => p.Customer)
            .ToListAsync();
    }

    private void ViewProjectDetails(Guid projectId)
    {
        navManager.NavigateTo($"/project/{projectId}");
    }
}
